class Particle {
  final double x, y, r, speed, phase, opacity, twinkleSpeed;

  const Particle({
    required this.x,
    required this.y,
    required this.r,
    required this.speed,
    required this.phase,
    required this.opacity,
    required this.twinkleSpeed,
  });
}
